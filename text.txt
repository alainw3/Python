text

  print (i*sum([1 * (10**(k)) for k in range(i)]))
     

import operator , itertools;
for i in range(1,int(input())) : #More than 2 lines will result in 0 score. Do not leave a blank line also
     print (i*int(sum(list(map(lambda x: x/10,itertools.accumulate(itertools.repeat(10,i),operator.mul))))))

  print (i*int(sum(list(map(lambda x:(x/x)*(10**x), range(1,i+1))))/10))